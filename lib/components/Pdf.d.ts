import { data, config, options } from '../types';
export declare const Pdf: ({ data, config, options }: PdfProps) => any;
export interface PdfProps {
    data?: data;
    config?: config;
    options?: options;
}
export default Pdf;
//# sourceMappingURL=Pdf.d.ts.map